---
import { cn } from '@lib/utils';
import { Image } from 'astro:assets';
import Button from '../ui/Button.astro';
import Tag from '../ui/Tag.astro';

interface Props {
  title: string;
  description: string;
  image?: string;
  href?: string;
  tags?: string[];
  variant?: 'default' | 'glass' | 'gradient';
  class?: string;
}

const {
  title,
  description,
  image,
  href,
  tags = [],
  variant = 'default',
  class: className
} = Astro.props;

const variants = {
  default: 'bg-white border border-neutral-200',
  glass: 'bg-white/50 backdrop-blur-sm border border-neutral-200/50',
  gradient: 'bg-gradient-to-br from-primary-50 to-secondary-50 border border-primary-200',
};
---

<div class={cn('rounded-xl overflow-hidden shadow-sm hover:shadow-lg transition-all duration-300 hover:-translate-y-1', variants[variant], href && 'cursor-pointer', className)}>
  {image && (
    <div class="aspect-video overflow-hidden">
      <Image
        src={image}
        alt={title}
        width={400}
        height={225}
        class="w-full h-full object-cover hover:scale-105 transition-transform duration-300"
      />
    </div>
  )}
  <div class="p-6">
    <h3 class="text-xl font-bold text-neutral-900 mb-2">
      {title}
    </h3>
    <p class="text-neutral-600 mb-4">
      {description}
    </p>
    {tags.length > 0 && (
      <div class="flex flex-wrap gap-2 mb-4">
        {tags.map((tag) => (
          <Tag>{tag}</Tag>
        ))}
      </div>
    )}
    {href && (
      <Button href={href} variant="primary">
        Learn More
      </Button>
    )}
  </div>
</div>
