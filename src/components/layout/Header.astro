---
import siteConfig from '../../../site-config.json';
import Navigation from './Navigation.astro';
import MobileMenu from './MobileMenu.astro';
import Button from '../ui/Button.astro';
import Container from '../sections/Container.astro';

const { site } = siteConfig;
---

<header class="sticky top-0 z-40 w-full border-b border-neutral-200 bg-white/80 backdrop-blur-md">
  <Container>
    <div class="flex h-16 items-center justify-between">
      <div class="flex items-center gap-8">
        <a href="/" class="text-lg md:text-xl font-bold text-neutral-900 truncate max-w-[200px] md:max-w-none">
          {site.name}
        </a>
        <Navigation />
      </div>

      <div class="flex items-center gap-4">
        <Button href="/contact" size="sm" class="hidden md:inline-flex">
          Get Started
        </Button>
        <MobileMenu />
      </div>
    </div>
  </Container>
</header>
